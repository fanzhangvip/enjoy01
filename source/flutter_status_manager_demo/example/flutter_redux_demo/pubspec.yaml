name: flutter_redux_demo
description: A new flutter module project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

  # 第一步 添加支持库
  redux: ^3.0.0
  flutter_redux: ^0.5.3
  # 添加支持库
  redux_thunk: ^0.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

  module:
    androidPackage: com.example.flutter_redux_demo
    iosBundleIdentifier: com.example.flutterReduxDemo
